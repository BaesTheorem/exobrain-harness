# Supernote OCR (transcript-processing/supernote-parser.py)
# 0.7.1 adds SN_FILE_VER_20260016 (Chauvet 3.28.42). Older pins raise
# UnsupportedFileFormat on any note the device has resaved under that firmware.
supernotelib==0.7.1
# Weather (weather/get-weather.py -- numpy used by Open-Meteo SDK return values)
openmeteo-requests==1.7.5
openmeteo-sdk==1.26.0
numpy
# Pomodoro desktop app (pomodoro/main.py + create_icon.py)
pywebview
pillow
