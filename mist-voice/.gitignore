# Voice DATA is NOT in this public harness. It's copyrighted Pantheon show audio
# and lives in a SEPARATE PRIVATE repo. See VOICE-DATA.md for the repo + rebuild.
#   → https://github.com/BaesTheorem/mist-voice-data  (private, git-lfs)
# Everything under samples/ (raw corpus, reference clips, curation TSVs) + the
# canonical sample is pulled from there, never committed here.
samples/
demo_mist.wav

# Pretrained PUBLIC model weights (~1.8GB XTTS + whisper) — auto-redownloaded.
models/

# Transient synthesis outputs
out.wav

# Python env
.venv/
__pycache__/
