# Captured RF/IR/NFC signals and SD backups are personal data -- never commit.
sd-backup/
*.sub
*.ir
*.nfc
*.rfid
*.png

.ble_addr

# ir/ holds files from public databases (Flipper-IRDB) with provenance headers,
# not captures of Alex's own remotes, so it is tracked.
!ir/*.ir
