#!/usr/bin/env python3
"""meetup: unofficial CLI for meetup.com (search events, browse groups, read event details,
RSVP from the terminal). Run `meetup -h` for commands.
"""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

from meetupcli.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
