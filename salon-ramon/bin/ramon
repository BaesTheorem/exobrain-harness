#!/usr/bin/env python3
"""ramon: book a haircut at Salon Ramon from the terminal. Run `ramon -h`."""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent))

from rosy.cli import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
