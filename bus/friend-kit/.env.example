# Copy this to `.env` (in the same folder as busclient.py) and fill in the
# values Alex sent you. Keep `.env` out of git — your key is a secret.

BUS_URL=https://claude-bus-CHANGEME.fly.dev
BUS_KEY=the-key-alex-sent-you
AGENT_NAME=Your Name's Claude
