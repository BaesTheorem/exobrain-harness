# Secrets & credentials
.mcp.json
.env
.claude/channels/
.claude/settings.local.json

# MIST persona portraits (copyrighted Pantheon stills, local-only)
.claude/MIST*.webp
.claude/mist.png

# Claude Code runtime
.claude/projects/
.claude/plugins/
.claude/session-memories/
# session memories also land at repo root; keep both ignored (personal context)
session-memories/
# a session once wrote to the singular path by mistake; ignore it too so the
# auto-commit can never sweep personal context into the public repo
session-memory/
.claude/worktrees/

# OS files
.DS_Store

# Python
__pycache__/
*.pyc
.venv/

# Phone -- Google Calendar OAuth credentials + token (secrets)
phone/gcp-oauth.keys.json
phone/gcal-token.json

# Personal/sensitive data
.claude/skills/health/health-concerns-config.md
cycle-tracker/cycle-data.json
cycle-tracker/cycle-data.archived-*.json
cycle-tracker/app.py
local-events/local-events-log.json
local-events/local-events-prefs.json
sailboat-retro/retro-data.json
discord/discord-digest-fetch.py
reddit/data/
.claude/skills/cybersecurity-bodyguard/targets.json
.claude/skills/cybersecurity-bodyguard/scan-results/
.claude/skills/cybersecurity-bodyguard/cache/

# Claude Bus -- secrets + local state
bus/.env
bus/*.db
bus/.bus-cursor
bus/friend-kit/.env

# Runtime state
processing-log.json
discord/discord-digest.json
awair/state.json
awair/air-log.csv
.claude/hooks/state/
tv/token.json
tv/state.json
vault-migration-report.md
scratch/
recon/
apple-notes-sync/
.tmp-winddown-*.md

# solo-dm: campaign state (SQLite + JSONL), preferences, non-SRD rules content
data/solo-dm/
.claude/skills/solo-dm/data/extensions/*.json
!.claude/skills/solo-dm/data/extensions/README.md

# Writing-style corpus (raw personal writing -- private)
writing-style/corpus/

# Claude Bot -- private config (guild/channel IDs), local state, venv
claude-bot/config.toml
claude-bot/claudebot.db
claude-bot/claudebot.db-*
claude-bot/.venv/
claude-bot/.ace-venv/
claude-bot/.ace-cache/
claude-bot/assets/
claude-bot/**/__pycache__/

# Scratch / temp extraction (junk -- never commit)
tmp-cannonballs/

# disposable-email: alias registry (personal) + API secrets
disposable-email/aliases.json
disposable-email/secrets.json

# Transient flag: headless job scan couldn't reach LinkedIn MCP (see job-search/README.md)
job-search/.linkedin-scan-pending
job-search/.gmail-scan-pending

# resume-builder: real resume data + per-JD tailoring contain Alex's PII /
# resume content (public repo). Ship only the *.example.* templates.
resume-builder/data/resume_data.json
resume-builder/tailoring/*
!resume-builder/tailoring/example.json
!resume-builder/tailoring/example_cover.md

# Scratch images/screenshots -- temp working files, never commit
tmp/
# Generated images (mist-image) -- personal, never commit
mist-image/gallery/
# Reference face for --ref identity generation: a copyrighted show frame, never commit
mist-image/mist-reference-face.png
# Generated + input audio (mist-music) -- personal, never commit
mist-music/out/
# mist-voice: chatterbox venv + generated audio outputs -- large binaries, never commit
mist-voice/.venv-chatterbox/
mist-voice/*.wav
# Loose audio/video files dropped at repo root -- large binaries, never commit
/*.mp3
/*.wav
/*.mp4
/*.m4a

# AirDrop->Console watcher runtime
airdrop-to-console/state.json
airdrop-to-console/watch.log

# iMessage synced cache (chat.db snapshot + contacts + real names) -- never commit
imessage/cache/
imessage/logs/

# Wi-Fi diagnostics output (SSID, MAC, gateway, DNS) -- never commit
maintenance/wifi-diag-out.txt

# Local-only watcher fleet (personal product/price/tour targets, home ZIP,
# state, and its own README, which names them all). Nothing inside is tracked;
# .claude/commands/bounty.md and the root README note why the dir is absent.
# (Moved here from .git/info/exclude, which does not survive a re-clone.)
watchers/
substack-sync/sync.log
substack-sync/launchd.out.log
substack-sync/launchd.err.log

# Unemployment (UInteract) -- browser session, screenshots, and claim data are all personal
unemployment/.browser-profile/
unemployment/.chrome-profile/
unemployment/screenshots/
unemployment/data/
unemployment/answers*.json

# Group scheduler runtime (friends' calendar feed URLs, cached free/busy,
# in-flight event state) -- personal, never commit. Template + code are tracked.
scheduler/feeds.json
scheduler/freebusy-cache.json
scheduler/events/

# job-search: ATS watchlist snapshots + employer list (personal -- reveals where
# Alex applies). See job-search/state/README.md for rebuild.
job-search/state/*
!job-search/state/README.md

# Compiled EventKit helper (rebuilt on demand by reminders/add-reminder)
reminders/.build/

# Haircut history is a personal log; the cadence rebuilds from the last entry.
barber/state.json

# Booksy login session (browser profile). Never commit a live session.
barber/.booksy-profile/
barber/steps/

# Live Booksy session token, captured from the browser. Never commit.
barber/.booksy-session.json

# Fantasy football -- ESPN session cookies (espn_s2 + SWID)
fantasy/espn-credentials.json

# Dedicated FDA-granted interpreter (built locally, see maintenance/README.md)
maintenance/.venv/
