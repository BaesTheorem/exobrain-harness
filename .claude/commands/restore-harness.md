---
description: Restore the Exobrain harness onto a wiped or new Mac from the daily collective backup + GitHub + cloud re-auth. Vault is left to Obsidian Sync unless explicitly opted in. Modes: --minimal, --vault-from-backup, --dry-run, --no-watchers.
argument-hint: [--minimal | --vault-from-backup | --dry-run | --no-watchers]
---
Invoke the **restore-harness** skill (via the Skill tool) to handle the request below, then follow that skill's instructions. If no request is given, run the skill's default (full restore, vault left untouched per the guardrail).

Request: $ARGUMENTS
