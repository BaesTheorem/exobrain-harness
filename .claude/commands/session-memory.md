---
description: Cross-session continuity and context-aware data prioritization.
argument-hint: [request]
---
Invoke the **session-memory** skill (via the Skill tool) to handle the request below, then follow that skill's instructions. If no request is given, run the skill's default behavior.

Request: $ARGUMENTS
