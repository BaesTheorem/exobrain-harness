---
description: Multi-agent deep research system for complex questions requiring thorough investigation.
argument-hint: [request]
---
Invoke the **deep-research** skill (via the Skill tool) to handle the request below, then follow that skill's instructions. If no request is given, run the skill's default behavior.

Request: $ARGUMENTS
