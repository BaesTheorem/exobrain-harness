---
description: Play Old School RuneScape as MIST (character MISTci) on the Alora private server, fully in the background — log in, read game state, navigate, act, and talk in chat.
argument-hint: [what MIST should do]
---
Invoke the **osrs** skill (via the Skill tool) to handle the request below, then follow that skill's instructions. If no request is given, run the skill's default behavior (log MISTci into the world).

Request: $ARGUMENTS
