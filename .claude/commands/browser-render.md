---
description: Render HTML/SVG to a PNG/PDF image without flashing the screen.
argument-hint: [request]
---
Invoke the **browser-render** skill (via the Skill tool) to handle the request below, then follow that skill's instructions. If no request is given, run the skill's default behavior.

Request: $ARGUMENTS
