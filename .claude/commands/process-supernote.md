---
description: OCR Supernote handwritten notes by extracting PNG pages and reading them with vision.
argument-hint: [request]
---
Invoke the **process-supernote** skill (via the Skill tool) to handle the request below, then follow that skill's instructions. If no request is given, run the skill's default behavior.

Request: $ARGUMENTS
