---
description: Discover upcoming Kansas City events Alex would enjoy.
argument-hint: [request]
---
Invoke the **local-events** skill (via the Skill tool) to handle the request below, then follow that skill's instructions. If no request is given, run the skill's default behavior.

Request: $ARGUMENTS
