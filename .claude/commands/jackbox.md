---
description: Play Jackbox games autonomously by driving jackbox.tv in a headed Chrome over the DevTools protocol.
argument-hint: [request]
---
Invoke the **jackbox** skill (via the Skill tool) to handle the request below, then follow that skill's instructions. If no request is given, run the skill's default behavior.

Request: $ARGUMENTS
