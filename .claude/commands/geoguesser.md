---
description: Geolocate a photo or play GeoGuessr at a competitive level -- read a scene down to a country and then a region from bollards, road lines, camera generation, script, vegetation and sun angle, backed by a shadow-to-latitude solver, Street View coverage lookup, Overpass and scoring math.
argument-hint: [image, coordinates, or question]
---
Invoke the **geoguesser** skill (via the Skill tool) to handle the request below, then follow that skill's instructions. If no request is given, run the skill's default behavior.

Request: $ARGUMENTS
