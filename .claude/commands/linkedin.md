---
description: Best practices and conventions for all LinkedIn MCP interactions.
argument-hint: [request]
---
Invoke the **linkedin** skill (via the Skill tool) to handle the request below, then follow that skill's instructions. If no request is given, run the skill's default behavior.

Request: $ARGUMENTS
