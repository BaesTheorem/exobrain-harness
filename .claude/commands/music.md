---
description: Alex's music toolset -- generate songs, render/transcribe sheet music, and build captioned lyric videos.
argument-hint: [request]
---
Invoke the **music** skill (via the Skill tool) to handle the request below, then follow that skill's instructions. If no request is given, run the skill's default behavior.

Request: $ARGUMENTS
