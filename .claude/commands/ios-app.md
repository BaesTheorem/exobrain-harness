---
description: Build, sign, install, and debug iOS (and companion macOS) apps from the command line -- Xcode setup, XcodeGen projects, xcconfig, code…
argument-hint: [request]
---
Invoke the **ios-app** skill (via the Skill tool) to handle the request below, then follow that skill's instructions. If no request is given, run the skill's default behavior.

Request: $ARGUMENTS
