---
description: Work on Alex's self-contained D&D 5e character sheet web app (the MPMB replacement, single index.html).
argument-hint: [request]
---
Invoke the **dnd-sheet** skill (via the Skill tool) to handle the request below, then follow that skill's instructions. If no request is given, summarize the project's current state (read VERSION + the changelog top) and ask what to work on.

Request: $ARGUMENTS
