---
description: Remove AI-generated patterns from text to make it sound more human and authentic.
argument-hint: [request]
---
Invoke the **de-ai** skill (via the Skill tool) to handle the request below, then follow that skill's instructions. If no request is given, run the skill's default behavior.

Request: $ARGUMENTS
