---
description: Run extended multi-agent reconnaissance sessions.
argument-hint: [request]
---
Invoke the **deep-recon** skill (via the Skill tool) to handle the request below, then follow that skill's instructions. If no request is given, run the skill's default behavior.

Request: $ARGUMENTS
