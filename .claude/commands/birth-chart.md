---
description: Generate printable birth-chart PDF packets (natal + synastry).
argument-hint: [people and birth data]
---
Invoke the **birth-chart** skill (via the Skill tool) to handle the request below, then follow that skill's instructions. If no request is given, run the skill's default behavior.

Request: $ARGUMENTS
