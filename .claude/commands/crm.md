---
description: Look up, manage, and surface Network CRM contacts.
argument-hint: [request]
---
Invoke the **crm** skill (via the Skill tool) to handle the request below, then follow that skill's instructions. If no request is given, run the skill's default behavior.

Request: $ARGUMENTS
