---
description: Alex's player-side TTRPG assistant (NOT the GM skill).
argument-hint: [request]
---
Invoke the **ttrpg-player** skill (via the Skill tool) to handle the request below, then follow that skill's instructions. If no request is given, run the skill's default behavior.

Request: $ARGUMENTS
