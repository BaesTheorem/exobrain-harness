---
description: Pull, persist, and analyze health data from Fitbit and Withings.
argument-hint: [request]
---
Invoke the **health** skill (via the Skill tool) to handle the request below, then follow that skill's instructions. If no request is given, run the skill's default behavior.

Request: $ARGUMENTS
