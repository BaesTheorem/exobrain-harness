---
description: File and manage Alex's Missouri unemployment (UInteract) claim.
argument-hint: [request]
---
Invoke the **unemployment** skill (via the Skill tool) to handle the request below, then follow that skill's instructions. If no request is given, run the skill's default behavior.

Request: $ARGUMENTS
