---
description: Generate printable Human Design PDF packets (BodyGraph, Type, Strategy, Authority, Profile, Cross).
argument-hint: [person and birth data]
---
Invoke the **human-design** skill (via the Skill tool) to handle the request below, then follow that skill's instructions. If no request is given, run the skill's default behavior.

Request: $ARGUMENTS
