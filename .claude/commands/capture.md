---
description: Quick-capture a thought, task, note, or event.
argument-hint: [request]
---
Invoke the **capture** skill (via the Skill tool) to handle the request below, then follow that skill's instructions. If no request is given, run the skill's default behavior.

Request: $ARGUMENTS
