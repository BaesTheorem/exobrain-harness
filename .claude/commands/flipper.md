---
description: Drive Alex's Flipper Zero from the CLI — read/write files, analyze captures, transmit, over USB or Bluetooth.
argument-hint: [request]
---
Invoke the **flipper** skill (via the Skill tool) to handle the request below, then follow that skill's instructions. If no request is given, run the skill's default behavior.

Request: $ARGUMENTS
